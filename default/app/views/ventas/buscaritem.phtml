<?php 
	$producto = array (
					"idproducto" => "$productos->idproducto",
		 			"cod_producto" => "$productos->cod_producto",
		  			"descripcion" => "$productos->descripcion",
		  			"precio_venta" => "$productos->precio_venta",
		  			"exento" => "$productos->exento"
				); 
	echo json_encode($producto);
?>

